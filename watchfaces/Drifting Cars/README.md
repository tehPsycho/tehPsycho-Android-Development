# Drifting Cars Watchface Prototype

Open `clockface-prototype.html` in a browser to preview a basic concept where a car orbits the dial as the seconds indicator.

## Behavior
- The car moves clockwise once per minute (seconds-based motion).
- The car is tangent to the circular path to suggest drifting motion.
- The three car images are rotated through by second.
